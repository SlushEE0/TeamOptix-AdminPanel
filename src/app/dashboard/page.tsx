// import { isMobile } from "react-device-detect";

import DataWrapper from "./DataWrapper";

export default function Dashboard() {
  // return isMobile ? <p>Hello</p> : <DataWrapper />;
  return <DataWrapper />;
}
